version: '3.8'

services:
  mysql:
    image: mysql:8
    container_name: mimo-mysql
    environment:
      MYSQL_DATABASE: mimo
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: